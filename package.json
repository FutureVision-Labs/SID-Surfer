{
  "name": "sid-surfer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "package": "npm run build && powershell -Command \"Compress-Archive -Path dist\\* -DestinationPath sid-surfer-itch.zip -Force\""
  },
  "devDependencies": {
    "vite": "^7.2.2"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
